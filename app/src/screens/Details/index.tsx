import React from 'react';
import Styled from './styles';

const Details: React.FC = () => {
  return (
    <Styled.Container>
      <Styled.Text>Details screen</Styled.Text>
    </Styled.Container>
  );
};

export default Details;
